{
  "name": "reactexercises",
  "version": "1.0.0",
  "description": "Some exercises done while learning JavaScript",
  "main": "index.js",
  "scripts": {
    "clean": "rm -rf build && mkdir build",
    "build": "npm run clean && babel -d ./build ./src -s",
    "start": "npm run build && node ./src/mapexample.js"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "babel-preset-es2015": "^6.24.1"
  },
  "dependencies": {
    "babel-cli": "^6.26.0"
  }
}
